%li.row
  = gravatar_for user, size: 50
  = link_to user.name, user_path(user), class: 'user-text'
  = link_to "delete", admin_user_url(user), method: :delete, data: { confirm: "You sure?" }, class: 'user-text'